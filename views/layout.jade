!!!
html(lang='en')
  head
    include includes/head

  body
    .container
        #menu
            #left-menu
                a#draw-point.pure-button
                    i.icon-map-marker
                a#draw-line.pure-button
                    i.icon-pencil
                a#draw-shape.pure-button
                    i.icon-sign-blank
            #top-menu
                a#critical.pure-button.selected CRITICAL
                a#overlay.pure-button OVERLAY
            #right-menu
                a#critical.pure-button
                    i.icon-plus
                br
                a#analyiser.pure-button
                    i.icon-minus
            #bottom-menu
                a#team.pure-button TEAM
                a#weather.pure-button WEATHER
                a#feed.pure-button FEED

        block content

    | !{cachify_js('/components/jquery/jquery.min.js')}
    | !{cachify_js('/components/bootstrap/docs/assets/js/bootstrap.min.js')}
    | !{cachify_js('/components/leaflet/dist/leaflet.js')}
    | !{cachify_js('/components/leaflet-locatecontrol/src/L.Control.Locate.js')}
    | !{cachify_js('/components/underscore/underscore-min.js')}
    | !{cachify_js('/components/backbone/backbone-min.js')}
    | !{cachify_js('/js/primus.js')}
    | !{cachify_js('/js/site.min.js')}
    block scripts
